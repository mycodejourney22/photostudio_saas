<!-- app/views/appointments/show.html.erb (Complete file with all sections) -->
<% content_for :title, "Appointment ##{@appointment.id} - #{current_tenant.name}" %>

<div class="min-h-screen bg-gray-50 flex">
  <!-- Sidebar - Desktop -->
  <div class="hidden lg:flex lg:flex-shrink-0">
    <div class="flex flex-col w-64">
      <div class="flex-1 flex flex-col min-h-0 bg-gradient-to-b from-blue-600 to-purple-700">
        <%= render 'shared/sidebar' %>
      </div>
    </div>
  </div>

  <!-- Mobile sidebar overlay -->
  <div class="lg:hidden" data-controller="mobile-navigation">
    <!-- Mobile menu button -->
    <div class="fixed top-4 left-4 z-50">
      <button data-action="mobile-navigation#toggleMenu"
              class="bg-white p-2 rounded-lg shadow-lg text-gray-600 hover:text-gray-900 focus:outline-none focus:ring-2 focus:ring-blue-500">
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/>
        </svg>
      </button>
    </div>

    <!-- Mobile backdrop -->
    <div data-mobile-navigation-target="backdrop"
         data-action="click->mobile-navigation#closeOnBackdrop"
         class="hidden fixed inset-0 bg-black bg-opacity-50 z-40"></div>

    <!-- Mobile sidebar -->
    <div data-mobile-navigation-target="menu"
         class="fixed inset-y-0 left-0 z-50 w-64 bg-gradient-to-b from-blue-600 to-purple-700 shadow-lg transform -translate-x-full transition-transform duration-300 ease-in-out">
      <%= render 'shared/sidebar' %>
    </div>
  </div>

  <!-- Main content -->
  <div class="flex-1 flex flex-col overflow-hidden">
    <!-- Header -->
    <header class="bg-white shadow-sm border-b border-gray-200 flex-shrink-0">
      <div class="px-4 lg:px-6 py-4">
        <div class="flex items-center justify-between">
          <div class="flex items-center space-x-4 ml-12 lg:ml-0">
            <%= link_to appointments_path, class: "text-gray-500 hover:text-gray-700" do %>
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"/>
              </svg>
            <% end %>
            <div>
              <h1 class="text-2xl font-bold text-gray-900">Appointment #<%= @appointment.id %></h1>
              <p class="text-sm text-gray-600">
                <%= @appointment.customer.full_name %> •
                <%= @appointment.scheduled_at.strftime("%B %d, %Y at %I:%M %p") %>
              </p>
            </div>
          </div>

          <div class="flex items-center space-x-2">
            <!-- Status Badge -->
            <span class="inline-flex items-center px-3 py-1 rounded-full text-sm font-medium
                   <%= case @appointment.status
                       when 'confirmed' then 'bg-green-100 text-green-800'
                       when 'pending' then 'bg-yellow-100 text-yellow-800'
                       when 'in_progress' then 'bg-blue-100 text-blue-800'
                       when 'completed' then 'bg-purple-100 text-purple-800'
                       when 'cancelled' then 'bg-red-100 text-red-800'
                       else 'bg-gray-100 text-gray-800'
                       end %>">
              <%= @appointment.status.humanize %>
            </span>

            <!-- Payment Status Badge -->
            <span class="inline-flex items-center px-3 py-1 rounded-full text-sm font-medium
                   <%= case @appointment.payment_status
                       when 'paid' then 'bg-green-100 text-green-800'
                       when 'partial_paid' then 'bg-yellow-100 text-yellow-800'
                       when 'unpaid' then 'bg-red-100 text-red-800'
                       when 'refunded' then 'bg-gray-100 text-gray-800'
                       else 'bg-gray-100 text-gray-800'
                       end %>">
              <%= @appointment.payment_status.humanize %>
            </span>
          </div>
        </div>
      </div>
    </header>

    <!-- Main Content -->
    <main class="flex-1 overflow-auto">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
          <!-- Left Column: Main Details -->
          <div class="lg:col-span-2 space-y-6">

            <!-- Customer Information -->
            <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6">
              <h3 class="text-lg font-medium text-gray-900 mb-4">Customer Information</h3>
              <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                <div>
                  <label class="block text-sm font-medium text-gray-700">Name</label>
                  <p class="mt-1 text-sm text-gray-900 font-medium">
                    <%= link_to @appointment.customer.full_name, customer_path(@appointment.customer),
                          class: "text-blue-600 hover:text-blue-500" %>
                  </p>
                </div>
                <div>
                  <label class="block text-sm font-medium text-gray-700">Email</label>
                  <p class="mt-1 text-sm text-gray-900">
                    <% if @appointment.customer.email.present? %>
                      <%= link_to @appointment.customer.email, "mailto:#{@appointment.customer.email}",
                            class: "text-blue-600 hover:text-blue-500" %>
                    <% else %>
                      <span class="text-gray-400">Not provided</span>
                    <% end %>
                  </p>
                </div>
                <div>
                  <label class="block text-sm font-medium text-gray-700">Phone</label>
                  <p class="mt-1 text-sm text-gray-900">
                    <% if @appointment.customer.phone.present? %>
                      <%= link_to @appointment.customer.phone, "tel:#{@appointment.customer.phone}",
                            class: "text-blue-600 hover:text-blue-500" %>
                    <% else %>
                      <span class="text-gray-400">Not provided</span>
                    <% end %>
                  </p>
                </div>
                <div>
                  <label class="block text-sm font-medium text-gray-700">Customer Since</label>
                  <p class="mt-1 text-sm text-gray-900">
                    <%= @appointment.customer.created_at.strftime("%B %Y") %>
                  </p>
                </div>
              </div>
            </div>

            <!-- Session Details -->
            <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6">
              <h3 class="text-lg font-medium text-gray-900 mb-4">Session Details</h3>
              <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                <div>
                  <label class="block text-sm font-medium text-gray-700">Service Package</label>
                  <p class="mt-1 text-sm text-gray-900"><%= @appointment.service_package_name || 'Not specified' %></p>
                </div>
                <div>
                  <label class="block text-sm font-medium text-gray-700">Session Type</label>
                  <p class="mt-1 text-sm text-gray-900 capitalize"><%= @appointment.session_type.humanize %></p>
                </div>
                <div>
                  <label class="block text-sm font-medium text-gray-700">Duration</label>
                  <p class="mt-1 text-sm text-gray-900"><%= @appointment.duration_minutes %> minutes</p>
                </div>
                <div>
                  <label class="block text-sm font-medium text-gray-700">Base Price</label>
                  <p class="mt-1 text-sm text-gray-900 font-medium">$<%= @appointment.price %></p>
                </div>
                <div>
                  <label class="block text-sm font-medium text-gray-700">Location</label>
                  <p class="mt-1 text-sm text-gray-900">
                    <% if @appointment.studio_location.present? %>
                      <%= @appointment.studio_location.name %>
                    <% elsif @appointment.studio.present? %>
                      <%= @appointment.studio.name %>
                    <% else %>
                      <span class="text-gray-400">Not assigned</span>
                    <% end %>
                  </p>
                </div>
                <div>
                  <label class="block text-sm font-medium text-gray-700">Scheduled</label>
                  <p class="mt-1 text-sm text-gray-900">
                    <%= @appointment.scheduled_at.strftime("%B %d, %Y at %I:%M %p") %>
                  </p>
                </div>
              </div>

              <% if @appointment.notes.present? || @appointment.special_requirements.present? %>
                <div class="mt-4 pt-4 border-t border-gray-200">
                  <% if @appointment.notes.present? %>
                    <div class="mb-3">
                      <label class="block text-sm font-medium text-gray-700">Notes</label>
                      <p class="mt-1 text-sm text-gray-900"><%= @appointment.notes %></p>
                    </div>
                  <% end %>
                  <% if @appointment.special_requirements.present? %>
                    <div>
                      <label class="block text-sm font-medium text-gray-700">Special Requirements</label>
                      <p class="mt-1 text-sm text-gray-900"><%= @appointment.special_requirements %></p>
                    </div>
                  <% end %>
                </div>
              <% end %>
            </div>

            <!-- Staff Assignment Section -->
            <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6"
                 data-controller="staff-assignment"
                 data-staff-assignment-appointment-id-value="<%= @appointment.id %>">
              <h3 class="text-lg font-medium text-gray-900 mb-4">Staff Assignment</h3>

              <!-- Photographer Assignment -->
              <div class="mb-6">
                <div class="flex items-center justify-between mb-2">
                  <label class="block text-sm font-medium text-gray-700">Photographer</label>
                  <% if @photographers.any? %>
                    <div class="relative">
                      <select data-staff-assignment-target="photographerSelect"
                              data-action="change->staff-assignment#assignStaff"
                              data-assignment-type="photographer"
                              class="text-xs px-2 py-1 border border-gray-300 rounded focus:outline-none focus:ring-1 focus:ring-blue-500">
                        <option value="">Assign photographer...</option>
                        <% @photographers.each do |photographer| %>
                          <option value="<%= photographer.id %>"
                                  <%= 'selected' if @appointment.assigned_photographer_id == photographer.id %>>
                            <%= photographer.full_name %>
                          </option>
                        <% end %>
                      </select>
                    </div>
                  <% end %>
                </div>

                <div data-staff-assignment-target="photographerDisplay">
                  <% if @appointment.assigned_photographer.present? %>
                    <div class="p-2 bg-blue-50 rounded-lg">
                      <div class="flex items-center justify-between">
                        <div class="flex items-center">
                          <div class="w-6 h-6 bg-blue-100 rounded-full flex items-center justify-center mr-2">
                            <svg class="w-3 h-3 text-blue-600" fill="currentColor" viewBox="0 0 20 20">
                              <path fill-rule="evenodd" d="M4 5a2 2 0 00-2 2v8a2 2 0 002 2h12a2 2 0 002-2V7a2 2 0 00-2-2h-1.586a1 1 0 01-.707-.293l-1.121-1.121A2 2 0 0011.172 3H8.828a2 2 0 00-1.414.586L6.293 4.707A1 1 0 015.586 5H4zm6 9a3 3 0 100-6 3 3 0 000 6z" clip-rule="evenodd"/>
                            </svg>
                          </div>
                          <div class="flex-1">
                            <p class="text-xs font-medium text-blue-900"><%= @appointment.assigned_photographer.full_name %></p>
                            <p class="text-xs text-blue-700">Currently assigned</p>
                          </div>
                        </div>
                        <button type="button"
                                data-action="click->staff-assignment#unassignStaff"
                                data-assignment-type="photographer"
                                class="text-xs text-blue-600 hover:text-blue-800">
                          Remove
                        </button>
                      </div>
                    </div>
                  <% else %>
                    <div class="space-y-2">
                      <span class="text-sm text-gray-500">No photographer assigned</span>
                      <% if @photographers.any? %>
                        <div class="mt-2 space-x-1">
                          <% @photographers.limit(3).each do |photographer| %>
                            <button type="button"
                                    class="inline-flex items-center px-2 py-1 bg-blue-100 text-blue-700 text-xs font-medium rounded hover:bg-blue-200 transition-colors"
                                    data-action="click->staff-assignment#quickAssign"
                                    data-staff-id="<%= photographer.id %>"
                                    data-assignment-type="photographer">
                              <%= photographer.first_name %>
                            </button>
                          <% end %>
                        </div>
                      <% end %>
                    </div>
                  <% end %>
                </div>
              </div>

              <!-- Editor Assignment -->
              <div>
                <div class="flex items-center justify-between mb-2">
                  <label class="block text-sm font-medium text-gray-700">Editor</label>
                  <% if @editors.any? %>
                    <div class="relative">
                      <select data-staff-assignment-target="editorSelect"
                              data-action="change->staff-assignment#assignStaff"
                              data-assignment-type="editor"
                              class="text-xs px-2 py-1 border border-gray-300 rounded focus:outline-none focus:ring-1 focus:ring-blue-500">
                        <option value="">Assign editor...</option>
                        <% @editors.each do |editor| %>
                          <option value="<%= editor.id %>"
                                  <%= 'selected' if @appointment.assigned_editor_id == editor.id %>>
                            <%= editor.full_name %>
                          </option>
                        <% end %>
                      </select>
                    </div>
                  <% end %>
                </div>

                <div data-staff-assignment-target="editorDisplay">
                  <% if @appointment.assigned_editor.present? %>
                    <div class="p-2 bg-purple-50 rounded-lg">
                      <div class="flex items-center justify-between">
                        <div class="flex items-center">
                          <div class="w-6 h-6 bg-purple-100 rounded-full flex items-center justify-center mr-2">
                            <svg class="w-3 h-3 text-purple-600" fill="currentColor" viewBox="0 0 20 20">
                              <path d="M13.586 3.586a2 2 0 112.828 2.828l-.793.793-2.828-2.828.793-.793zM11.379 5.793L3 14.172V17h2.828l8.38-8.379-2.83-2.828z"/>
                            </svg>
                          </div>
                          <div class="flex-1">
                            <p class="text-xs font-medium text-purple-900"><%= @appointment.assigned_editor.full_name %></p>
                            <p class="text-xs text-purple-700">Currently assigned</p>
                          </div>
                        </div>
                        <button type="button"
                                data-action="click->staff-assignment#unassignStaff"
                                data-assignment-type="editor"
                                class="text-xs text-purple-600 hover:text-purple-800">
                          Remove
                        </button>
                      </div>
                    </div>
                  <% else %>
                    <div class="space-y-2">
                      <span class="text-sm text-gray-500">No editor assigned</span>
                      <% if @editors.any? %>
                        <div class="mt-2 space-x-1">
                          <% @editors.limit(3).each do |editor| %>
                            <button type="button"
                                    class="inline-flex items-center px-2 py-1 bg-purple-100 text-purple-700 text-xs font-medium rounded hover:bg-purple-200 transition-colors"
                                    data-action="click->staff-assignment#quickAssign"
                                    data-staff-id="<%= editor.id %>"
                                    data-assignment-type="editor">
                              <%= editor.first_name %>
                            </button>
                          <% end %>
                        </div>
                      <% end %>
                    </div>
                  <% end %>
                </div>
              </div>

              <!-- Assignment Tips -->
              <div class="mt-4 p-3 bg-gray-50 rounded-lg">
                <div class="flex">
                  <svg class="w-5 h-5 text-gray-400 mr-2 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
                  </svg>
                  <div>
                    <p class="text-sm text-gray-700 font-medium">Assignment Tips</p>
                    <ul class="mt-1 text-xs text-gray-600 space-y-1">
                      <li>• Photographers handle the actual photo shoot</li>
                      <li>• Editors process and enhance the photos after the shoot</li>
                      <li>• Staff will be automatically notified of their assignments</li>
                      <li>• You can change assignments at any time before the appointment</li>
                    </ul>
                  </div>
                </div>
              </div>
            </div>

            <!-- Sales & Purchases Section -->
            <%= render 'appointments/sales_section', appointment: @appointment %>

            <!-- Production Timeline -->
            <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6">
              <h3 class="text-lg font-medium text-gray-900 mb-4">Production Timeline</h3>
              <div class="flow-root">
                <ul class="-mb-8">
                  <!-- Appointment Created -->
                  <li>
                    <div class="relative pb-8">
                      <span class="absolute top-4 left-4 -ml-px h-full w-0.5 bg-gray-200" aria-hidden="true"></span>
                      <div class="relative flex space-x-3">
                        <div>
                          <span class="h-8 w-8 rounded-full bg-blue-500 flex items-center justify-center ring-8 ring-white">
                            <svg class="w-4 h-4 text-white" fill="currentColor" viewBox="0 0 20 20">
                              <path fill-rule="evenodd" d="M6 2a1 1 0 00-1 1v1H4a2 2 0 00-2 2v10a2 2 0 002 2h12a2 2 0 002-2V6a2 2 0 00-2-2h-1V3a1 1 0 10-2 0v1H7V3a1 1 0 00-1-1zm0 5a1 1 0 000 2h8a1 1 0 100-2H6z" clip-rule="evenodd"/>
                            </svg>
                          </span>
                        </div>
                        <div class="min-w-0 flex-1 pt-1.5">
                          <div>
                            <p class="text-sm text-gray-900">Appointment scheduled</p>
                            <p class="mt-0.5 text-xs text-gray-500">
                              <%= @appointment.created_at.strftime("%B %d, %Y at %I:%M %p") %>
                            </p>
                          </div>
                        </div>
                      </div>
                    </div>
                  </li>

                  <!-- Main Sale Created -->
                  <% if @appointment.main_sale.present? %>
                    <li>
                      <div class="relative pb-8">
                        <span class="absolute top-4 left-4 -ml-px h-full w-0.5 bg-gray-200" aria-hidden="true"></span>
                        <div class="relative flex space-x-3">
                          <div>
                            <span class="h-8 w-8 rounded-full bg-green-500 flex items-center justify-center ring-8 ring-white">
                              <svg class="w-4 h-4 text-white" fill="currentColor" viewBox="0 0 20 20">
                                <path d="M4 4a2 2 0 00-2 2v4a2 2 0 002 2V6h10a2 2 0 00-2-2H4zm2 6a2 2 0 012-2h8a2 2 0 012 2v4a2 2 0 01-2 2H8a2 2 0 01-2-2v-4zm6 4a2 2 0 100-4 2 2 0 000 4z"/>
                              </svg>
                            </span>
                          </div>
                          <div class="min-w-0 flex-1 pt-1.5">
                            <div>
                              <p class="text-sm text-gray-900">
                                Main sale created (#<%= @appointment.main_sale.sale_number %>)
                              </p>
                              <p class="mt-0.5 text-xs text-gray-500">
                                <%= @appointment.main_sale.created_at.strftime("%B %d, %Y at %I:%M %p") %>
                              </p>
                            </div>
                          </div>
                        </div>
                      </div>
                    </li>
                  <% end %>

                  <!-- Photographer Assignment -->
                  <% if @appointment.assigned_photographer.present? %>
                    <li>
                      <div class="relative pb-8">
                        <span class="absolute top-4 left-4 -ml-px h-full w-0.5 bg-gray-200" aria-hidden="true"></span>
                        <div class="relative flex space-x-3">
                          <div>
                            <span class="h-8 w-8 rounded-full bg-purple-500 flex items-center justify-center ring-8 ring-white">
                              <svg class="w-4 h-4 text-white" fill="currentColor" viewBox="0 0 20 20">
                                <path fill-rule="evenodd" d="M4 5a2 2 0 00-2 2v8a2 2 0 002 2h12a2 2 0 002-2V7a2 2 0 00-2-2h-1.586a1 1 0 01-.707-.293l-1.121-1.121A2 2 0 0011.172 3H8.828a2 2 0 00-1.414.586L6.293 4.707A1 1 0 015.586 5H4zm6 9a3 3 0 100-6 3 3 0 000 6z" clip-rule="evenodd"/>
                              </svg>
                            </span>
                          </div>
                          <div class="min-w-0 flex-1 pt-1.5">
                            <div>
                              <p class="text-sm text-gray-900">
                                Photographer assigned: <%= @appointment.assigned_photographer.full_name %>
                              </p>
                              <p class="mt-0.5 text-xs text-gray-500">Ready for shoot</p>
                            </div>
                          </div>
                        </div>
                      </div>
                    </li>
                  <% end %>

                  <!-- Shoot Completion -->
                  <% if @appointment.shoot_completed_at.present? %>
                    <li>
                      <div class="relative pb-8">
                        <span class="absolute top-4 left-4 -ml-px h-full w-0.5 bg-gray-200" aria-hidden="true"></span>
                        <div class="relative flex space-x-3">
                          <div>
                            <span class="h-8 w-8 rounded-full bg-indigo-500 flex items-center justify-center ring-8 ring-white">
                              <svg class="w-4 h-4 text-white" fill="currentColor" viewBox="0 0 20 20">
                                <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"/>
                              </svg>
                            </span>
                          </div>
                          <div class="min-w-0 flex-1 pt-1.5">
                            <div>
                              <p class="text-sm text-gray-900">Shoot completed</p>
                              <p class="mt-0.5 text-xs text-gray-500">
                                <%= @appointment.shoot_completed_at.strftime("%B %d, %Y at %I:%M %p") %>
                              </p>
                            </div>
                          </div>
                        </div>
                      </div>
                    </li>
                  <% end %>

                  <!-- Additional Sales Timeline Items -->
                  <% @appointment.additional_sales.each do |sale| %>
                    <li>
                      <div class="relative pb-8">
                        <span class="absolute top-4 left-4 -ml-px h-full w-0.5 bg-gray-200" aria-hidden="true"></span>
                        <div class="relative flex space-x-3">
                          <div>
                            <span class="h-8 w-8 rounded-full bg-orange-500 flex items-center justify-center ring-8 ring-white">
                              <svg class="w-4 h-4 text-white" fill="currentColor" viewBox="0 0 20 20">
                                <path d="M3 4a1 1 0 011-1h12a1 1 0 011 1v2a1 1 0 01-1 1H4a1 1 0 01-1-1V4zM3 10a1 1 0 011-1h6a1 1 0 011 1v6a1 1 0 01-1 1H4a1 1 0 01-1-1v-6zM14 9a1 1 0 00-1 1v6a1 1 0 001 1h2a1 1 0 001-1v-6a1 1 0 00-1-1h-2z"/>
                              </svg>
                            </span>
                          </div>
                          <div class="min-w-0 flex-1 pt-1.5">
                            <div>
                              <p class="text-sm text-gray-900">
                                Additional purchase (#<%= sale.sale_number %>) - $<%= sale.total_amount %>
                              </p>
                              <p class="mt-0.5 text-xs text-gray-500">
                                <%= sale.created_at.strftime("%B %d, %Y at %I:%M %p") %>
                              </p>
                            </div>
                          </div>
                        </div>
                      </div>
                    </li>
                  <% end %>

                  <!-- Editor Assignment -->
                  <% if @appointment.assigned_editor.present? %>
                    <li>
                      <div class="relative pb-8">
                        <span class="absolute top-4 left-4 -ml-px h-full w-0.5 bg-gray-200" aria-hidden="true"></span>
                        <div class="relative flex space-x-3">
                          <div>
                            <span class="h-8 w-8 rounded-full bg-yellow-500 flex items-center justify-center ring-8 ring-white">
                              <svg class="w-4 h-4 text-white" fill="currentColor" viewBox="0 0 20 20">
                                <path d="M13.586 3.586a2 2 0 112.828 2.828l-.793.793-2.828-2.828.793-.793zM11.379 5.793L3 14.172V17h2.828l8.38-8.379-2.83-2.828z"/>
                              </svg>
                            </span>
                          </div>
                          <div class="min-w-0 flex-1 pt-1.5">
                            <div>
                              <p class="text-sm text-gray-900">
                                Editor assigned: <%= @appointment.assigned_editor.full_name %>
                              </p>
                              <p class="mt-0.5 text-xs text-gray-500">Ready for editing</p>
                            </div>
                          </div>
                        </div>
                      </div>
                    </li>
                  <% end %>

                  <!-- Editing Completion -->
                  <% if @appointment.editing_completed_at.present? %>
                    <li>
                      <div class="relative pb-8">
                        <span class="absolute top-4 left-4 -ml-px h-full w-0.5 bg-gray-200" aria-hidden="true"></span>
                        <div class="relative flex space-x-3">
                          <div>
                            <span class="h-8 w-8 rounded-full bg-teal-500 flex items-center justify-center ring-8 ring-white">
                              <svg class="w-4 h-4 text-white" fill="currentColor" viewBox="0 0 20 20">
                                <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"/>
                              </svg>
                            </span>
                          </div>
                          <div class="min-w-0 flex-1 pt-1.5">
                            <div>
                              <p class="text-sm text-gray-900">Editing completed</p>
                              <p class="mt-0.5 text-xs text-gray-500">
                                <%= @appointment.editing_completed_at.strftime("%B %d, %Y at %I:%M %p") %>
                              </p>
                            </div>
                          </div>
                        </div>
                      </div>
                    </li>
                  <% end %>

                  <!-- Delivery -->
                  <% if @appointment.delivery_date.present? %>
                    <li>
                      <div class="relative">
                        <div class="relative flex space-x-3">
                          <div>
                            <span class="h-8 w-8 rounded-full <%= @appointment.delivery_date <= Date.current ? 'bg-emerald-500' : 'bg-gray-400' %> flex items-center justify-center ring-8 ring-white">
                              <svg class="w-4 h-4 text-white" fill="currentColor" viewBox="0 0 20 20">
                                <path d="M8 16.5a1.5 1.5 0 11-3 0 1.5 1.5 0 013 0zM15 16.5a1.5 1.5 0 11-3 0 1.5 1.5 0 013 0z"/>
                                <path d="M3 4a1 1 0 00-1 1v10a1 1 0 001 1h1.05a2.5 2.5 0 014.9 0H10a1 1 0 001-1V5a1 1 0 00-1-1H3zM14 7a1 1 0 00-1 1v6.05A2.5 2.5 0 0115.95 16H17a1 1 0 001-1V8a1 1 0 00-1-1h-3z"/>
                              </svg>
                            </span>
                          </div>
                          <div class="min-w-0 flex-1 pt-1.5">
                            <div>
                              <p class="text-sm text-gray-900">
                                <%= @appointment.delivery_date <= Date.current ? 'Delivered' : 'Scheduled for delivery' %>
                              </p>
                              <p class="mt-0.5 text-xs text-gray-500">
                                <%= @appointment.delivery_date.strftime("%B %d, %Y") %>
                              </p>
                            </div>
                          </div>
                        </div>
                      </div>
                    </li>
                  <% else %>
                    <!-- Future steps -->
                    <% unless @appointment.status == 'completed' %>
                      <li>
                        <div class="relative">
                          <div class="relative flex space-x-3">
                            <div>
                              <span class="h-8 w-8 rounded-full bg-gray-400 flex items-center justify-center ring-8 ring-white">
                                <svg class="w-4 h-4 text-white" fill="currentColor" viewBox="0 0 20 20">
                                  <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm1-11a1 1 0 10-2 0v2H7a1 1 0 100 2h2v2a1 1 0 102 0v-2h2a1 1 0 100-2h-2V7z" clip-rule="evenodd"/>
                                </svg>
                              </span>
                            </div>
                            <div class="min-w-0 flex-1 pt-1.5">
                              <div>
                                <p class="text-sm text-gray-500">Delivery pending...</p>
                                <p class="mt-0.5 text-xs text-gray-400">Will be scheduled after editing completion</p>
                              </div>
                            </div>
                          </div>
                        </div>
                      </li>
                    <% end %>
                  <% end %>
                </ul>
              </div>
            </div>

          </div>

          <!-- Right Column: Actions & Summary -->
          <div class="space-y-6">
            <!-- Quick Actions -->
            <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6">
              <h3 class="text-lg font-medium text-gray-900 mb-4">Quick Actions</h3>
              <div class="space-y-3">



                <!-- Send Message -->

                <!-- Production Actions -->
                <% if @appointment.assigned_photographer.present? && !@appointment.shoot_completed? %>
                  <%= button_to mark_shoot_completed_appointment_path(@appointment),
                        method: :patch,
                        class: "w-full inline-flex items-center justify-center px-4 py-2 bg-purple-600 text-white text-sm font-medium rounded-lg hover:bg-purple-700 transition-colors",
                        data: { confirm: "Mark shoot as completed?" } do %>
                    <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
                    </svg>
                    Mark Shoot Complete
                  <% end %>
                <% end %>

                <% if @appointment.assigned_editor.present? && @appointment.shoot_completed? && !@appointment.editing_completed? %>
                  <%= button_to mark_editing_completed_appointment_path(@appointment),
                        method: :patch,
                        class: "w-full inline-flex items-center justify-center px-4 py-2 bg-teal-600 text-white text-sm font-medium rounded-lg hover:bg-teal-700 transition-colors border-0",
                        style: "background-color: #0d9488 !important; color: white !important;",
                        data: { confirm: "Mark editing as completed?" } do %>
                    <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
                    </svg>
                    Mark Editing Complete
                  <% end %>
                <% end %>

                <!-- Sales Actions -->
                <div class="border-t border-gray-200 pt-3">
                  <% unless @appointment.has_main_sale? %>
                  <%= link_to new_sale_path(appointment_id: @appointment.id),
                          method: :post,
                          params: { appointment_id: @appointment.id },
                          class: "mt-4 inline-flex items-center px-4 py-2 bg-blue-600 text-white text-sm font-medium rounded-lg hover:bg-blue-700" do %>
                      <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"/>
                      </svg>
                      Create Main Sale
                    <% end %>
                  <% else %>
                    <%= link_to new_sale_path(appointment_id: @appointment.id, additional: true),
                          class: "w-full inline-flex items-center justify-center px-4 py-2 bg-green-600 text-white text-sm font-medium rounded-lg hover:bg-green-700" do %>
                      <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"/>
                      </svg>
                      Add Additional Sale
                    <% end %>
                  <% end %>
                </div>

                <!-- Cancel Appointment -->
                <% if @appointment.can_be_cancelled? %>
                  <%= link_to cancel_appointment_path(@appointment),
                        method: :patch,
                        class: "w-full inline-flex items-center justify-center px-4 py-2 bg-red-600 text-white text-sm font-medium rounded-lg hover:bg-red-700",
                        data: { confirm: "Are you sure you want to cancel this appointment?" } do %>
                    <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
                    </svg>
                    Cancel Appointment
                  <% end %>
                <% end %>
              </div>
            </div>

            <!-- Sales Summary -->
            <% if @appointment.sales.any? %>
              <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6">
                <h3 class="text-lg font-medium text-gray-900 mb-4">Sales Summary</h3>
                <div class="space-y-3">
                  <div class="flex justify-between text-sm">
                    <span class="text-gray-600">Number of Sales:</span>
                    <span class="font-medium"><%= @appointment.sales.count %></span>
                  </div>
                  <div class="flex justify-between text-sm">
                    <span class="text-gray-600">Total Revenue:</span>
                    <span class="font-medium">$<%= number_with_precision(@appointment.total_sales_amount, precision: 2) %></span>
                  </div>
                  <div class="flex justify-between text-sm">
                    <span class="text-gray-600">Amount Paid:</span>
                    <span class="font-medium text-green-600">$<%= number_with_precision(@appointment.sales.sum(:paid_amount), precision: 2) %></span>
                  </div>
                  <div class="flex justify-between text-sm border-t border-gray-200 pt-2">
                    <span class="text-gray-600">Outstanding Balance:</span>
                    <span class="font-medium <%= (@appointment.total_sales_amount - @appointment.sales.sum(:paid_amount)) > 0 ? 'text-red-600' : 'text-green-600' %>">
                      $<%= number_with_precision(@appointment.total_sales_amount - @appointment.sales.sum(:paid_amount), precision: 2) %>
                    </span>
                  </div>
                </div>

                <!-- View All Sales Link -->
                <div class="mt-4 pt-4 border-t border-gray-200">
                  <%= link_to sales_path(appointment_id: @appointment.id),
                        class: "text-blue-600 hover:text-blue-800 text-sm font-medium" do %>
                    View All Sales →
                  <% end %>
                </div>
              </div>
            <% end %>

            <!-- Customer Summary -->
            <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6">
              <h3 class="text-lg font-medium text-gray-900 mb-4">Customer Summary</h3>
              <div class="space-y-3">
                <div class="flex justify-between text-sm">
                  <span class="text-gray-600">Total Appointments:</span>
                  <span class="font-medium"><%= @appointment.customer.appointments.count %></span>
                </div>
                <div class="flex justify-between text-sm">
                  <span class="text-gray-600">Total Spent:</span>
                  <span class="font-medium">$<%= number_with_precision(@appointment.customer.total_revenue || 0, precision: 2) %></span>
                </div>
                <div class="flex justify-between text-sm">
                  <span class="text-gray-600">Last Visit:</span>
                  <span class="font-medium">
                    <% if @appointment.customer.last_appointment&.scheduled_at %>
                      <%= time_ago_in_words(@appointment.customer.last_appointment.scheduled_at) %> ago
                    <% else %>
                      This appointment
                    <% end %>
                  </span>
                </div>
              </div>

              <!-- View Customer Profile -->
              <div class="mt-4 pt-4 border-t border-gray-200">
                <%= link_to @appointment.customer,
                      class: "text-blue-600 hover:text-blue-800 text-sm font-medium" do %>
                  View Customer Profile →
                <% end %>
              </div>
            </div>

            <!-- Production Status -->
            <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6">
              <h3 class="text-lg font-medium text-gray-900 mb-4">Production Status</h3>
              <div class="space-y-4">

                <!-- Photographer Status -->
                <div class="flex items-center justify-between">
                  <span class="text-sm text-gray-600">Photographer:</span>
                  <% if @appointment.assigned_photographer.present? %>
                    <span class="inline-flex items-center px-2 py-1 bg-blue-100 text-blue-800 text-xs font-medium rounded-full">
                      ✓ Assigned
                    </span>
                  <% else %>
                    <span class="inline-flex items-center px-2 py-1 bg-gray-100 text-gray-800 text-xs font-medium rounded-full">
                      Pending
                    </span>
                  <% end %>
                </div>

                <!-- Shoot Status -->
                <div class="flex items-center justify-between">
                  <span class="text-sm text-gray-600">Shoot:</span>
                  <% if @appointment.shoot_completed_at.present? %>
                    <span class="inline-flex items-center px-2 py-1 bg-green-100 text-green-800 text-xs font-medium rounded-full">
                      ✓ Completed
                    </span>
                  <% elsif @appointment.status == 'in_progress' %>
                    <span class="inline-flex items-center px-2 py-1 bg-yellow-100 text-yellow-800 text-xs font-medium rounded-full">
                      In Progress
                    </span>
                  <% else %>
                    <span class="inline-flex items-center px-2 py-1 bg-gray-100 text-gray-800 text-xs font-medium rounded-full">
                      Scheduled
                    </span>
                  <% end %>
                </div>

                <!-- Editor Status -->
                <div class="flex items-center justify-between">
                  <span class="text-sm text-gray-600">Editor:</span>
                  <% if @appointment.assigned_editor.present? %>
                    <span class="inline-flex items-center px-2 py-1 bg-purple-100 text-purple-800 text-xs font-medium rounded-full">
                      ✓ Assigned
                    </span>
                  <% else %>
                    <span class="inline-flex items-center px-2 py-1 bg-gray-100 text-gray-800 text-xs font-medium rounded-full">
                      Pending
                    </span>
                  <% end %>
                </div>

                <!-- Editing Status -->
                <div class="flex items-center justify-between">
                  <span class="text-sm text-gray-600">Editing:</span>
                  <% if @appointment.editing_completed_at.present? %>
                    <span class="inline-flex items-center px-2 py-1 bg-green-100 text-green-800 text-xs font-medium rounded-full">
                      ✓ Completed
                    </span>
                  <% elsif @appointment.shoot_completed_at.present? %>
                    <span class="inline-flex items-center px-2 py-1 bg-yellow-100 text-yellow-800 text-xs font-medium rounded-full">
                      In Progress
                    </span>
                  <% else %>
                    <span class="inline-flex items-center px-2 py-1 bg-gray-100 text-gray-800 text-xs font-medium rounded-full">
                      Waiting
                    </span>
                  <% end %>
                </div>

                <!-- Delivery Status -->
                <div class="flex items-center justify-between">
                  <span class="text-sm text-gray-600">Delivery:</span>
                  <% if @appointment.delivery_date.present? %>
                    <% if @appointment.delivery_date <= Date.current %>
                      <span class="inline-flex items-center px-2 py-1 bg-green-100 text-green-800 text-xs font-medium rounded-full">
                        ✓ Delivered
                      </span>
                    <% else %>
                      <span class="inline-flex items-center px-2 py-1 bg-blue-100 text-blue-800 text-xs font-medium rounded-full">
                        <%= @appointment.delivery_date.strftime("%m/%d") %>
                      </span>
                    <% end %>
                  <% else %>
                    <span class="inline-flex items-center px-2 py-1 bg-gray-100 text-gray-800 text-xs font-medium rounded-full">
                      Not scheduled
                    </span>
                  <% end %>
                </div>
              </div>
            </div>

            <!-- Equipment & Notes -->
            <% if @appointment.equipment_used.present? || @appointment.production_notes.present? %>
              <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6">
                <h3 class="text-lg font-medium text-gray-900 mb-4">Production Details</h3>

                <% if @appointment.equipment_used.present? %>
                  <div class="mb-4">
                    <label class="block text-sm font-medium text-gray-700 mb-2">Equipment Used</label>
                    <p class="text-sm text-gray-900"><%= @appointment.equipment_used %></p>
                  </div>
                <% end %>

                <% if @appointment.production_notes.present? %>
                  <div>
                    <label class="block text-sm font-medium text-gray-700 mb-2">Production Notes</label>
                    <p class="text-sm text-gray-900"><%= @appointment.production_notes %></p>
                  </div>
                <% end %>
              </div>
            <% end %>

          </div>
        </div>
      </div>
    </main>
  </div>
</div>

<!-- JavaScript for Staff Assignment -->
<script>
document.addEventListener('DOMContentLoaded', function() {
  // Staff Assignment Controller
  class StaffAssignmentController {
    constructor(element) {
      this.element = element;
      this.appointmentId = element.dataset.staffAssignmentAppointmentIdValue;
      this.photographerSelect = element.querySelector('[data-staff-assignment-target="photographerSelect"]');
      this.editorSelect = element.querySelector('[data-staff-assignment-target="editorSelect"]');
      this.photographerDisplay = element.querySelector('[data-staff-assignment-target="photographerDisplay"]');
      this.editorDisplay = element.querySelector('[data-staff-assignment-target="editorDisplay"]');

      this.bindEvents();
    }

    bindEvents() {
      // Quick assign buttons
      this.element.addEventListener('click', (e) => {
        if (e.target.closest('[data-action*="quickAssign"]')) {
          e.preventDefault();
          const button = e.target.closest('[data-action*="quickAssign"]');
          const staffId = button.dataset.staffId;
          const assignmentType = button.dataset.assignmentType;
          this.assignStaff(staffId, assignmentType);
        }
      });

      // Unassign buttons
      this.element.addEventListener('click', (e) => {
        if (e.target.closest('[data-action*="unassignStaff"]')) {
          e.preventDefault();
          const button = e.target.closest('[data-action*="unassignStaff"]');
          const assignmentType = button.dataset.assignmentType;
          this.assignStaff('', assignmentType);
        }
      });

      // Select dropdowns
      if (this.photographerSelect) {
        this.photographerSelect.addEventListener('change', (e) => {
          this.assignStaff(e.target.value, 'photographer');
        });
      }

      if (this.editorSelect) {
        this.editorSelect.addEventListener('change', (e) => {
          this.assignStaff(e.target.value, 'editor');
        });
      }
    }

    async assignStaff(staffId, assignmentType) {
      try {
        const response = await fetch(`/appointments/${this.appointmentId}/assign_staff`, {
          method: 'PATCH',
          headers: {
            'Content-Type': 'application/json',
            'X-CSRF-Token': document.querySelector('meta[name="csrf-token"]').content
          },
          body: JSON.stringify({
            staff_member_id: staffId,
            assignment_type: assignmentType
          })
        });

        const result = await response.json();

        if (result.success) {
          // Reload the page to show updated assignments
          window.location.reload();
        } else {
          alert(result.message || 'Assignment failed');
        }
      } catch (error) {
        console.error('Assignment error:', error);
        alert('Assignment failed. Please try again.');
      }
    }
  }

  // Initialize staff assignment controllers
  document.querySelectorAll('[data-controller="staff-assignment"]').forEach(element => {
    new StaffAssignmentController(element);
  });
});
</script>

<!-- Include Alpine.js for any additional interactive components -->
<script defer src="https://unpkg.com/alpinejs@3.x.x/dist/cdn.min.js"></script>
